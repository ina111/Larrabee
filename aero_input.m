%スイープさせる変数を作り、それをforループさせて全部データマトリックスに入れる


%-------------------------------
%	条件入力
%-------------------------------
%R,B,r,chord,beta,は引き継ぎ
rpm = 120;						%回転数[rpm]
V = 7.0;						%機速[m/s]
rho = 1.184;					%空気密度ρ[kg/m^3]
nu = 1.540 * 10^(-5);			%動粘性係数ν[m^2/s]
zeta0(1,:) = linspace(0.2,0.2,length(r));

%値を変えるものを入れる
var = 'rpm';
var_start = 60;
var_end = 180;
var_delta = 10;